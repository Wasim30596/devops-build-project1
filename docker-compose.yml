services:
  react-app:
    image: devops-react-app:latest
    container_name: react-app
    ports:
      - "80:80"
    restart: unless-stopped

  nginx-exporter:
    image: nginx/nginx-prometheus-exporter:latest
    container_name: nginx-exporter
    ports:
      - "9113:9113"
    command:
      - --nginx.scrape-uri=http://react-app:80/stub_status
    depends_on:
      - react-app
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_SMTP_ENABLED=true
      - GF_SMTP_HOST=smtp.gmail.com:587
      - GF_SMTP_USER=wasim3096k@gmail.com
      - GF_SMTP_PASSWORD=ksxa tkoo leoa qbok
      - GF_SMTP_FROM_ADDRESS=wasim3096k@gmail.com
      - GF_SMTP_FROM_NAME=Grafana Alert
    volumes:
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    restart: unless-stopped

volumes:
  prometheus-data:
  grafana-data: